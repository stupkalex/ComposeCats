package com.example.composecats.core.mappers

import com.example.composecats.core.entity.CatEntity
import com.example.composecats.core.network.dto.CatDTO

object CatMapper {
    fun mapCatDtoToEntity(catDto: List<CatDTO>) : List<CatEntity> {
        return emptyList()
    }
}